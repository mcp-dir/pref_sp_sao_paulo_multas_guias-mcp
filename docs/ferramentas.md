# Ferramentas

Prefeitura SP São Paulo: Multas expõe 1 ferramenta (todas somente leitura).

### 1. `pref_sp_sao_paulo_multas_guias_consultar`
**Input**: `login_cpf` (opcional), `login_cnpj` (opcional), `login_senha`, `placa`, `renavam`, `ait_lista` (opcional), `ignora_boleto` (opcional)

Prefeitura SP São Paulo: Multas, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar Prefeitura SP São Paulo: Multas
```
